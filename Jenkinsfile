pipeline {

    agent any
	
	stages {
    	stage('Checkout code') {
        	steps {
            	checkout scm
            	
            	sh "sudo docker login -u 'shkrishnamoorthy' -p 'Un1que234\$' "
            	
        
        	}
        }
    }
    
}